@page
@model Helpdesk.Pages.TaskStatuses.EditModel

@{
    ViewData["Title"] = "Edit Task Status";
}

<h1>Edit</h1>

<h4>Task Status</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TaskStatus.Id" />
            <input type="hidden" asp-for="TaskStatus.IsSystemType" />
            <div class="form-group">
                <label asp-for="TaskStatus.Name" class="control-label"></label>
                <input asp-for="TaskStatus.Name" class="form-control" />
                <span asp-validation-for="TaskStatus.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TaskStatus.Description" class="control-label"></label>
                <input asp-for="TaskStatus.Description" class="form-control" />
                <span asp-validation-for="TaskStatus.Description" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="TaskStatus.IsCompleted" /> @Html.DisplayNameFor(model => model.TaskStatus.IsCompleted)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
