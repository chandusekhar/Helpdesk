@page
@model Helpdesk.Pages.DocumentTypes.DetailsModel

@{
    ViewData["Title"] = "Document Type Details";
}

<h1>Details</h1>

<div>
    <h4>DocumentType</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DocumentType.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DocumentType.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.DocumentType.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DocumentType.Description)
        </dd>
    </dl>
</div>
<div>
    @if (!@Model.DocumentType.IsSystemType)
    {
        <a asp-page="./Edit" asp-route-id="@Model.DocumentType?.Id">Edit</a> <text>|</text>
    }
    <a asp-page="./Index">Back to List</a>
</div>
