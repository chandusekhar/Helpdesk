@page
@model Helpdesk.Pages.TicketStatuses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Ticket Statuses</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TicketStatus[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketStatus[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketStatus[0].DisplayOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketStatus[0].IsCompleted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketStatus[0].Archived)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TicketStatus) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DisplayOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsCompleted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Archived)
            </td>
            <td>
                @if (!item.IsSystemType)
                {
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> <text> | </text>
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                }
            </td>
        </tr>
}
    </tbody>
</table>
