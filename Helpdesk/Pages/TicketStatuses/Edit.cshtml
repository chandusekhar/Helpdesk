@page
@model Helpdesk.Pages.TicketStatuses.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TicketStatus</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TicketStatus.Id" />
            <input type="hidden" asp-for="TicketStatus.IsSystemType" />
            <div class="form-group">
                <label asp-for="TicketStatus.Name" class="control-label"></label>
                <input asp-for="TicketStatus.Name" class="form-control" />
                <span asp-validation-for="TicketStatus.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TicketStatus.Description" class="control-label"></label>
                <input asp-for="TicketStatus.Description" class="form-control" />
                <span asp-validation-for="TicketStatus.Description" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="TicketStatus.IsCompleted" /> @Html.DisplayNameFor(model => model.TicketStatus.IsCompleted)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
